# CareFlow — Environment Variables
# Copy to .env and fill in your values

# Database (leave as sqlite for local dev; Railway sets this automatically in production)
DATABASE_URL=sqlite:///./careflow.db

# JWT Secret — change this in production!
SECRET_KEY=careflow-dev-secret-change-me
ACCESS_TOKEN_EXPIRE_MINUTES=480
